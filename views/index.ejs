<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="icon" type="image/x-icon" href="/favicon.ico">
        <link rel="stylesheet" href="/styles.css">
        <script src="/copy_button.js"></script> 
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
        <title>URL Shortener</title>
    </head>

<body>

    <div class="background-image"></div>

    <div class="container-full-height">

            <div class="container-big">

                <!-- Header Section -->
                <header class="text-center py-3 header">
                    <a href="https://savesome.space"><img src="logo.png" alt="logo" class="mb-3 logo"></a>
                    <h1 class="display-4 title">save some space.</h1>
                </header>

                <!-- Form Section -->
                <div class="container px-2 my-5">

                    <form id="url-form" action="/shortUrls" method="POST">
                        <input type="url" class="form-control mb-3" id="fullUrl" name="fullUrl" placeholder="example.com" required>

                        <div class="d-grid">
                            <button class="btn btn-primary btn-lg" id="submitButton" type="submit">shorten link</button>
                        </div>
                    </form>

                <!-- Shortened URL Section -->
                    <div class="text-center h4 shortened-url-display">
                    <% if (latestShortUrl) { %>
                        <div class="mb-2 mt-4 shortened-url" id="shortenedUrl"><a href="<%= latestShortUrl.short %>">savesome.space/<%= latestShortUrl.short %></a></div>
                        <div class="mt-3"><button class="btn btn-primary shortened-url-button" id="copyButton">copy to clipboard</button></div>
                    <% } %>
                    </div>

                </div>
            </div>

            <!-- Footer Section -->
            <div class="text-center">
                <div class="bottom-text">check out my projects on <a href="https://github.com/f4rys">github</a>.</div>
            </div>

    </div>

</body>
</html>

