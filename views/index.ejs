<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="icon" type="image/x-icon" href="/favicon.ico">
        <link rel="stylesheet" href="/styles.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
        <title>URL Shortener</title>
    </head>

<body>

    <div class="background-image"></div>

    <div class="container-full-height">

            <!-- Header Section -->

            <header class="text-center py-5 header">
                <img src="logo.png" alt="logo" class="mb-3">
                <h1 class="display-4">URL Shortener</h1>
            </header>

            <!-- Main Content Section -->

            <div class="container px-5 my-5">

                <form id="contactForm" action="/shortUrls" method="POST">
                    <div class="mb-3">
                        <input type="url" class="form-control" id="fullUrl" name="fullUrl" placeholder="Enter URL" required>
                        <div class="invalid-feedback" data-sb-feedback="enterUrl:required">Enter URL is required.</div>
                    </div>

                    <div class="d-grid">
                        <button class="btn btn-primary btn-lg" id="submitButton" type="submit">Submit</button>
                    </div>
                </form>

                <div class="mb-3">
                <% if (latestShortUrl) { %>

                    <div class="shortened-url text-center">Shortened URL: <a href="<%= latestShortUrl.short %>"><%= latestShortUrl.short %></a></div>

                <% } %>   
                </div>
            </div>

            <div class="col text-center">
                    <p>This is the text at the bottom of the container.</p>
            </div>


    </div>

</body>
</html>

